#!/bin/sh
set -eu
DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
case "$(basename -- "$0")" in
  hrns-line-audit) exec go run "$DIR/../cmd/hrns" line-audit "$@" ;;
  hrns-audit) exec go run "$DIR/../cmd/hrns" audit "$@" ;;
esac
exec go run "$DIR/../cmd/hrns" "$@"
